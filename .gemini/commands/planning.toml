description = "Break work into small verifiable tasks with acceptance criteria and dependency ordering"

prompt = """
Invoke the planning-and-task-breakdown skill. If a `strategist` custom subagent has been registered in the active Gemini environment, call it with the spec plus relevant codebase context. Otherwise apply agents/strategist.md in the main context and label the result as degraded single-context planning. `strategist` is the canonical planner for Viserys; keep the pass read-only and return its executable plan for human review.

Read the existing spec (SPEC.md or equivalent) and the relevant codebase sections. Then:

1. Enter plan mode — read only, no code changes
2. Identify the dependency graph between components
3. Slice work vertically (one complete path per task, not horizontal layers)
4. Write tasks with acceptance criteria and verification steps
5. Add checkpoints between phases
6. Present the plan for human review

Save the plan to tasks/plan.md and task list to tasks/todo.md.

If tasks/plan.md or tasks/todo.md already exists with unchecked tasks for different work, stop and ask before writing — never silently overwrite an incomplete plan.
"""
